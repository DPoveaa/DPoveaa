name: Generate Snake

on:
  schedule:
    - cron: "*/30 * * * *"  # atualiza a cada 30 minutos
  workflow_dispatch: 

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Generate Snake with Growth and Random Movement
        uses: Platane/snk@master
        with:
          github_user_name: DPoveaa
          outputs: |
            dist/github-snake.svg?color_snake=#00ff00&color_dots=#00ff00,#00cc00,#009900,#006600

      - name: Commit and push
        run: |
          git config --local user.name "GitHub Actions"
          git config --local user.email "actions@github.com"
          git add dist/github-snake.svg
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "🐍 Snake animation updated - $(date '+%Y-%m-%d %H:%M')"
            git push
          fi
